-flash.each do |key,msg|
  =javascript_tag do
    Materialize.toast("#{msg}", 5000, "#{key}")
  -#.alert.alert-dismissible{class: "alert-#{key}", role: 'alert'}
  -#  %button.close{ aria: { :label => "Close" }, data: { :dismiss => "alert" }, :type => "button"}
  -#    %span{"aria-hidden" => "true"} ×
  -#  %strong= "#{key}".upcase
  -#  = msg